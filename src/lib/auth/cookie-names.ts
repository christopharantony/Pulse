// No 'server-only' or 'next/headers' import here — this file must be importable from both
// the Edge middleware runtime and Node route handlers.
export const ACCESS_TOKEN_COOKIE = 'pulse_access_token';
export const REFRESH_TOKEN_COOKIE = 'pulse_refresh_token';
